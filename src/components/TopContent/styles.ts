import styled from 'styled-components';

export const Container = styled.div`
  display: flex;

  margin-bottom: 6rem;

  > h1 {
    font-size: 3.5rem;
    font-weight: 700;
    color: var(--black-color);
  }
`;
